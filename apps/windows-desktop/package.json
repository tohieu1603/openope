{
  "name": "agent-operis-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Agent Operis Windows Desktop App (Electron + NSIS)",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && electron dist-electron/main.js",
    "prebuild": "node scripts/download-cloudflared.mjs",
    "build:gateway": "cd ../.. && pnpm build",
    "build:bundle-gateway": "node bundle-gateway.js",
    "build:electron": "tsc -p tsconfig.json",
    "build:installer": "electron-builder --win --config electron-builder.yml",
    "build:installer:byteplus": "electron-builder --win --config electron-builder-byteplus.yml",
    "build": "pnpm build:gateway && pnpm build:bundle-gateway && pnpm build:electron && pnpm build:installer",
    "build:byteplus": "pnpm build:gateway && pnpm build:bundle-gateway && pnpm build:electron && pnpm build:installer:byteplus"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^25.1.0",
    "typescript": "^5.7.0"
  }
}
